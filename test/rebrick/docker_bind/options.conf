options {
        directory "/var/cache/bind";
        listen-on { any; };
        allow-recursion { any; };
        allow-query { any; };
        allow-query-cache { any; };
        allow-transfer {
                none;
        };
        allow-update {
                none;
        };
};